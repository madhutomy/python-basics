# Tuples are immutable lists
# If you want to store data that don't change throughout the life of a program, then store them in tuples
houston_coordinates = (29.7, 95.3)
print(houston_coordinates)
# even though you can’t modify a tuple, you can assign a new value to a variable that represents a tuple.
houston_coordinates = (30, 96)